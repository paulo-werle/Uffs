#include <iostream>

using namespace std;

int main() {
  int line, col;

  // Percorre enquanto tiver entradas de linhas e colunas
  while(cin >> line && cin >> col) {

    cout << line;
    cout << col;

    // // Lê informações dos containers
    // int grid[line][col];
    // for (int i = 0; i <= col; i++) {
    //   for(int y = 0; y <= line; y++) {
    //     cin >> grid[y][i];
    //   }
    // }

    // for (int i = 0; i <= col; i++) {
    //   for(int y = 0; y <= line; y++) {
    //     cout << grid[y][i];
    //   }
    // }
  }
}