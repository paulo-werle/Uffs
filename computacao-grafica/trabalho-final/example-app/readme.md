# Projeto de Exemplo com ThreeJsMap

## Iniciar aplicação

Instalar dependencias (colocado --legacy-peer-deps devido a versão do @material-ui)
```bash
  npm install --legacy-peer-deps
```

Executando projeto
```bash
  npm run dev
```